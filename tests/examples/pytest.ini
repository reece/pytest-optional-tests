[pytest]
markers:
  marked: conventionally marked function
optional_tests:
  opt1: optional test 1
  opt2: optional test 2
  opt3: optional test 3 (not actually used in tests)
  opt4: optional test 4 (not actually used in tests)
run_optional_tests: opt3
